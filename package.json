{
  "name": "countdown",
  "version": "1.0.0",
  "description": "Contador utiliado nas páginas de black e promoções dentro do e-commerce",
  "main": "index.js",
  "scripts": {
    "test": "npx jest",
    "test:watch": "npx jest --config jest.config.js --watchAll",
    "test:cov": "npx jest --config jest.config.js --coverage",
    "build": "tsc --build && webpack --config webpack.config.js && rm build/browser.js && rm build/browser.d.ts"
  },
  "keywords": [
    "countdown",
    "cronômetro",
    "timer"
  ],
  "author": "Alessandro Miranda",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^27.0.2",
    "@types/react": "^17.0.34",
    "jest": "^27.3.1",
    "ts-jest": "^27.0.7",
    "typescript": "^4.4.4",
    "webpack": "^5.60.0",
    "webpack-cli": "^4.9.1"
  }
}
